DATABASE_URL=""
# REDIS_URL=""
INCOMING_QUEUE="backend-to-engine-broker"
# Seed the insurance fund with this many USD on startup (dev/test only; leave unset in prod)
INSURANCE_FUND_SEED_USD=10000
# How often (ms) to write a full engine state snapshot to DB (default: 300000 = 5 min)
SNAPSHOT_INTERVAL_MS=300000